//: Playground - noun: a place where people can play

import UIKit

var length = 10
var width = 20
var height = 30

func volume (#length: Int, #width: Int, #height: Int) -> Int {
    return (length * width * height)
}

println(volume(length: 10, width: 20, height: 30))

var pic = img
println(pic)
